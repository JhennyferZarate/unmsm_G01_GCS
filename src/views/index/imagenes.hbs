<div class="container mt-5">
    <div class="card p-3">
        <div class="row">
            <div class="col-10">
                <input onChange={selectedHandler} class="form-control" type="file">
            </div>
            <div class="col-2">
                <button onClick={sendHandler} type="button" class="btn btn-primary col-12">
                    Upload
                </button>
            </div>
        </div>
        
    </div>
</div>

{{!-- <script src="js/imagenes.js"></script> --}}

<script>
    const [file,setFile] = useState(null);

    const selectedHandler = e => {
        setFile e.target.files[0];
    }

    const sendHandler = () => {
        if (!file){
            alert("demes subir imagen");
            return ;
        }
    }

</script>

